{
  "name": "foundation-browser-model-execution",
  "version": "1.0.0",
  "description": "Run `wasm-pack build` in root to build the WASM module.   Run `npm run start` in `www` to serve the site on http://localhost:8080/.",
  "main": "index.js",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "start": "webpack-dev-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@huggingface/transformers": "^3.4.1",
    "onnxruntime-web": "^1.21.0"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^13.0.0",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.1"
  }
}
